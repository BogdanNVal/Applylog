fastapi>=0.115
uvicorn>=0.30
itsdangerous>=2.2
pydantic>=2.9
email-validator>=2.2
# Binary wheel is what CI/Render use. On Windows with Smart App Control,
# that DLL gets blocked and psycopg falls back to pure Python.
psycopg[binary]>=3.2
psycopg-pool>=3.2
